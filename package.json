{
  "name": "food-delivery-app",
  "version": "1.0.0",
  "description": "Flutter food delivery app",
  "main": "lib/main.dart",
  "scripts": {
    "build": "flutter build web",
    "start": "flutter run -d web-server --web-hostname 0.0.0.0 --web-port 3000"
  },
  "dependencies": {},
  "devDependencies": {}
}
